{
  "name": "gestos",
  "version": "1.0.0",
  "description": "GestOS",
  "main": "index.js",
  "scripts": {
    "start": "npx browser-sync start --server --files .src/*.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Antony",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.29.3"
  }
}
